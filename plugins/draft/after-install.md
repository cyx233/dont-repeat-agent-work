# DRAFT installed

Commands available:

- `/draft-save` — Save work as a reusable script
- `/draft-note` — Save context as a reusable note  
- `/draft-rm <name>` — Delete a cached item

Cached items are stored in `.claude/scripts/` and `.claude/notes/`.

The plugin automatically scans for cached items on session start and injects them into context.
